/*
*	minNum = INT_MAX, num = 0;		//最少变化次数，当前变化次数
*	int minMutation(string start, string end, vector<string>& bank) {
		递归中止条件：
		if(start == end){
			minNum = min(minNum, num)
			return minNum;
		}
		否则的话

	}
*/